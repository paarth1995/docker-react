language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t paasharma03/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true paarth1995/docker-react npm run test